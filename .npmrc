# Preserve one module identity for React and every peer consumer. The hoisted
# linker confuses this workspace's `react` directory with the React dependency.
node-linker=isolated
# Shadow resolves transitive JS imports from the root node_modules directory.
# Public links expose those packages without copying React into each consumer.
shamefully-hoist=true
